// Online C compiler to run C program online
#include <stdio.h>

int main() {
    float T [5];
    int i;
    for(i=0 ; i<5 ; i++) {
    printf("entrez le nombre %d : ",i+1);
    scanf("%f" , &T[i]);
}
    return 0;
}